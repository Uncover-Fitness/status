name: Response Time CI
on:
  schedule:
    - cron: "0 23 * * *"
  workflow_dispatch:
jobs:
  response-time:
    runs-on: ubuntu-latest
    steps:
      - name: Run Upptime Response Time
        uses: upptime/response-time@v1
        with:
          token: ${{ secrets.GH_PAT }}
