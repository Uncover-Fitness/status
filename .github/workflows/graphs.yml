name: Graphs CI
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
jobs:
  graphs:
    runs-on: ubuntu-latest
    steps:
      - name: Run Upptime Graphs
        uses: upptime/graphs@v1
        with:
          token: ${{ secrets.GH_PAT }}
